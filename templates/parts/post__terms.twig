{% if terms %}
  <div class="post__terms button-group">
  {% for term in terms %}
    <a class="post__terms_link button button--secondary" href="{{term.link}}">{{term.name}}</a>
  {% endfor %}
  </div>
{% endif %}
